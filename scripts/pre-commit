#!/bin/sh
# 커밋 전 Checkstyle 검사

echo "🔍 Running Checkstyle..."
./gradlew checkstyleMain --daemon -q

if [ $? -ne 0 ]; then
    echo "❌ Checkstyle 위반이 있습니다. 수정 후 다시 커밋하세요."
    echo "   리포트: build/reports/checkstyle/main.html"
    exit 1
fi

echo "✅ Checkstyle passed"